# Dockerfile simplificado para desarrollo
FROM node:22-alpine

WORKDIR /usr/src/app

# Variables de entorno
ENV NODE_ENV=development
ENV PORT=3000

# Exponer puerto
EXPOSE 3000

# El código y node_modules vienen del bind mount
# Solo ejecutar next dev
CMD ["npm", "run", "dev"]
